# Non-secret configuration specific to the TED backend
BACKEND=ted
# assure all telemetry is tagged with the backend it came from.
# In case we do enable telemetry in future.
OTEL_RESOURCE_ATTRIBUTES=rap.backend=ted

# the DNS for this is managed by NIoTs environment.
# the TLS is self signed, and generated by us, given to NIoT tech folk to add
# to trust store on RDP environment.
RELEASE_HOST=https://airlock.ted
DJANGO_ALLOWED_HOSTS=airlock.ted

# overrides for ted - we don't want to use the default controller
CONTROLLER_TASK_API_ENDPOINT=https://controller.ted.bennettoxford.org
AIRLOCK_API_ENDPOINT=https://jobs.ted.bennettoxford.org
# Note: we're not using the collector currently
COLLECTOR_ADDRESS=https://collector.ted.bennettoxford.org
DOCKER_PROXY=docker-proxy.ted.bennettoxford.org/opensafely-core
GITHUB_PROXY_DOMAIN=github-proxy.ted.bennettoxford.org
# allow niot org repos only
ALLOWED_GITHUB_ORGS=niot-ted
